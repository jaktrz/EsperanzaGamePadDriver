<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools"
	package="pl.cinek.esperanzagamepaddriver">

	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
	<uses-permission android:name="android.permission.INTERNET"/>

	<uses-feature
		android:name="android.software.leanback"
		android:required="false"/>
	<uses-feature
		android:name="android.hardware.touchscreen"
		android:required="false"/>

	<application
		android:name="androidx.multidex.MultiDexApplication"
		android:allowBackup="true"
		android:banner="@mipmap/ic_banner"
		android:icon="@mipmap/ic_launcher"
		android:logo="@mipmap/ic_banner"
		android:label="@string/app_name"
		android:requestLegacyExternalStorage="true"
		android:supportsRtl="true"
		android:theme="@style/AppThemeDark"
		tools:ignore="AllowBackup,GoogleAppIndexingWarning,UnusedAttribute,IntentFilterExportedReceiver">
		<activity
			android:name="MainActivity"
			android:configChanges="orientation|screenSize|uiMode|touchscreen|smallestScreenSize|screenLayout|navigation|mnc|mcc|locale|layoutDirection|keyboardHidden|keyboard|fontScale|density|colorMode"
			android:launchMode="singleTask">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity
			android:name="TestActivity"
			android:configChanges="orientation|screenSize|uiMode|touchscreen|smallestScreenSize|screenLayout|navigation|mnc|mcc|locale|layoutDirection|keyboardHidden|keyboard|fontScale|density|colorMode"
			android:label="@string/gamepadTest"
			android:launchMode="singleTask"/>
		<activity
			android:name="SettingsActivity"
			android:configChanges="orientation|screenSize|uiMode|touchscreen|smallestScreenSize|screenLayout|navigation|mnc|mcc|locale|layoutDirection|keyboardHidden|keyboard|fontScale|density|colorMode"
			android:label="@string/settings"
			android:launchMode="singleTask"/>

		<service
			android:name="MyInputMethodService"
			android:permission="android.permission.BIND_INPUT_METHOD">
			<intent-filter>
				<action android:name="android.view.InputMethod"/>
			</intent-filter>
			<meta-data
				android:name="android.view.im"
				android:resource="@xml/input_method"/>
		</service>
		<service
			android:name="MyAccessibilityService"
			android:exported="true"
			android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
			<intent-filter>
				<action android:name="android.accessibilityservice.AccessibilityService"/>
			</intent-filter>
			<meta-data
				android:name="android.accessibilityservice"
				android:resource="@xml/accessibility_service_config"/>
		</service>
	</application>

</manifest>